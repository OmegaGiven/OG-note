<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.og_note"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <!--
          hardwareAccelerated=false: on at least one real device (Galaxy S24,
          Adreno 750), Android opts this app into Vulkan-backed HWUI
          rendering, and that GPU driver intermittently fails to build its
          rendering pipeline on launch, a permanent blank white screen, not
          a crash (touch input keeps working; nothing paints). Tried
          avoiding the Vulkan opt-in via targetSdk instead (see
          build.gradle.kts) so hardware acceleration could stay on, but the
          white screen still recurred under real use. This blunt flag,
          software-render everything so there is no Vulkan pipeline to
          fail, is the only approach that has held up under repeated
          testing. Trade-off: typing/scrolling is slower than with GPU
          rendering.
        -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true"
            android:hardwareAccelerated="false"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
